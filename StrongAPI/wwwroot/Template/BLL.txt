@Model.usingstr
using SqlSugar;
using System;
using System.Collections.Generic;
namespace @Model.ClassNamespace
{
	public class @(Model.Name)BLL : BaseBussiness<@(Model.Name),int>, I@(Model.Name)BLL
	{
 
	//当前类已经继承了增、删、查、改的方法

	//这里面写的代码不会给覆盖,如果要重新生成请删除 @(Model.Name)BLL.cs


	I@(Model.Name)Repository  _dal;
        public @(Model.Name)BLL()(I@(Model.Name)Repository dal)
        {
            this._dal = dal;
            base.BaseDal = dal;
        }
 
	}
}